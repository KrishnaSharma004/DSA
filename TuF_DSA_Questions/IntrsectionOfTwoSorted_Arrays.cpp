#include <iostream>
#include <vector>
using namespace std;
void unionarray(vector<int> &a,vector<int> &b){
    int n1=a.size();
    int n2=b.size();

}
int main(){
    int n1;
    cin >> n1;
    vector<int> a(n1);
    int n2;
    cin >> n2;
    vector<int> b(n2);
    for(int i=0;i<n1;++i){
        cin >> a[i];
    }
    for(int i=0;i<n2;++i){
        cin >> b[i];
    }
    unionarray(a1,a2);
    return 0;
}